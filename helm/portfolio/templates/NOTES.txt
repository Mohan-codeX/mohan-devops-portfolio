Thank you for installing the Mohan DevOps Portfolio Helm chart!

Release Information
-------------------
Release Name: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Chart: {{ .Chart.Name }} {{ .Chart.Version }}

{{- if .Values.ingress.enabled }}

The application is exposed through an Ingress.

Host:
  http://{{ .Values.ingress.host }}

{{- if .Values.ingress.tls.enabled }}

TLS is enabled.

HTTPS URL:
  https://{{ .Values.ingress.host }}

{{- else }}

TLS is disabled.

{{- end }}

{{- else }}

Ingress is disabled.

To access the application locally:

kubectl port-forward svc/{{ include "portfolio.fullname" . }}-reverse-proxy 8080:{{ .Values.reverseProxy.service.port }} -n {{ .Release.Namespace }}

Then open:

http://localhost:8080

{{- end }}

Useful Commands
---------------

View all resources:

kubectl get all -n {{ .Release.Namespace }}

View PersistentVolumeClaims:

kubectl get pvc -n {{ .Release.Namespace }}

Check Pods:

kubectl get pods -n {{ .Release.Namespace }}

Describe a Pod:

kubectl describe pod <pod-name> -n {{ .Release.Namespace }}

View Logs:

kubectl logs <pod-name> -n {{ .Release.Namespace }}

Upgrade the release:

helm upgrade {{ .Release.Name }} {{ .Chart.Name }}

Uninstall the release:

helm uninstall {{ .Release.Name }}
